= simple_form_for @job, class: "form-horizontal" do |f|
  = f.input :title, label: "Job title"
  = f.input :description
  = f.input :company, label: "Company name"
  = f.input :url, label: "Link to job post"
  = f.submit
